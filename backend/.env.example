# OpenAI API key for image captioning and embeddings
OPENAI_API_KEY=your_openai_key_here

# Supabase configuration
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_key

# Demo mode (set to "true" to allow anonymous uploads without authentication)
# When false (default), authentication is required for uploads and modifications
# WARNING: Keep this false in production to prevent unauthorized access
DEMO_MODE=false
